# Phase 17 — V2 Script Gate: Narrow Continuation

The prior run was externally terminated by the managed session's five-minute
ceiling before writing outputs. Continue the authorization and exact scope in
`phase-17-what-is-aepoch-v2-script.md`.

Work directly with no delegation. Finish within this run. Do not repeat broad
repository exploration or preflight; those were completed and proposal facts
are already established.

Required work only:

1. Record Chris's explicit 2026-08-05 `approved` response in the proposal
   artifact/checkpoint and append-only decision log. Concept C1, Remotion,
   atelier, provider approach, and $3 ceiling are approved; optional music
   generation remains deferred.
2. Locate V1's actual human-narration word alignment/transcript and build the
   schema-valid V2 `artifacts/script.json` from those exact spoken words and
   timestamps. Consolidate them into 12-16 substantial editorial sections.
   Do not rewrite the spoken narration, copy V1's provisional timing, or create
   32 micro-scenes.
3. Add the required top-level voice-performance plan, meaningful editorial
   enhancement cues, delivery cues, pronunciation guides where applicable,
   and self-review evidence.
4. Validate proposal, decision log, and script.
5. Write `checkpoint_script.json` as `awaiting_human`,
   `human_approved: false`; ensure proposal is completed/approved.
6. Stop. No scene plan, media call, audio processing, asset work, or compose.

Do not return analysis or a plan. Return only after the files exist and have
been verified, or report one exact blocker.
