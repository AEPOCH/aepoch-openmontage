# Phase 16 Handoff — Recraft Batch Reconciliation and Correction Plan

Continue project `aepoch-blog-pilot-what-is-aepoch` on branch `aepoch-series`.

This is a documentation and planning tranche. Do not make a paid generation
call until Chris explicitly approves the resulting correction plan.

## Read before acting

1. `AGENT_GUIDE.md` and the current durable knowledge state/log
2. `pipeline_defs/animated-explainer.yaml`
3. `skills/pipelines/explainer/asset-director.md`
4. `docs/aepoch-production-playbook/prompts/phase-16-recraft-visual-plate-batch.md`
5. `projects/aepoch-blog-pilot-what-is-aepoch/assets/images/recraft-plate-batch-review.md`
6. the contact sheet and all eight generated batch plates at native resolution
7. the current asset inventory, provisional manifest, checkpoint, decision log,
   and cost log

## Binding Monty review decision

Reconcile the batch record to the independent review dated 2026-08-03:

- accept Plate 1 `hook-1b` (radial composition is creatively useful);
- accept Plates 3, 4, 7, and 8 as already recorded;
- accept Plate 9 `build-5b` (inset reads as doorway depth, not ornament);
- reject only Plate 5 `build-1a` and Plate 6 `build-2a` for the dominant nested
  contour/topographic texture failure;
- keep Plates 10–12 explicitly ungenerated.

Update the provisional asset manifest, reuse mappings, counts, blocked/unblocked
scene list, and contact-sheet labels without hiding or deleting the original
Claude self-review. Preserve append-only decisions and honest generation costs.
Do not mark the assets checkpoint complete.

## Prepare the smallest correction plan

Develop a concrete plan for resolving Plates 5 and 6 and completing Plates
10–12. The plan must compare these two in-scope approaches:

1. A single Recraft V4 correction probe on one rejected plate, followed only
   after visual approval by the other missing plates.
2. Deterministic Remotion-native geometry for concepts that are inherently
   simple diagrams/icons, reserving Recraft only for editorial human/community
   illustration where generation materially improves the result.

For each approach, identify exact scene mappings, visual-coherence risks,
number of paid calls, worst-case incremental cost, and how it respects the
approved asset-first hybrid direction. Recommend one approach. Do not execute
either approach yet.

The recommendation should favor repeatability and semantic clarity over paying
for generated geometry that Remotion can reproduce exactly. It must still
honestly flag any change to the previously approved generated-plate inventory
as a production decision requiring Chris's approval.

## Evidence and stop

Produce a concise correction-plan review beside the other image reviews and
update durable knowledge/log. Commit and push only intended tracked changes.
Then stop with:

- reconciled accepted/rejected inventory;
- recommended correction path;
- exact maximum incremental cost;
- the one explicit approval Chris must give to proceed.

Do not call Recraft or any other paid provider. Do not generate images, TTS,
music, SFX, diagrams, or video. Do not modify narration, author composition,
render, publish, or deploy.
